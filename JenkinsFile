pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/Nuth68/MidthermDevOps.git'
            }
        }

        stage('Build') {
            steps {
                sh './mvnw clean install -DskipTests'
            }
        }

        stage('Run') {
            steps {
                sh './mvnw spring-boot:run &'
            }
        }
    }
}